## IOS-Views

This is a demo of rendering SwiftUI views within a React Native project.

```bash
npm install
cd ios && pod install
npx react-native run-ios
# or
yarn install
cd ios && pod install
yarn run ios
```
